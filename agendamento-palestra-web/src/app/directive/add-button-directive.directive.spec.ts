import { AddButtonDirectiveDirective } from './add-button-directive.directive';

describe('AddButtonDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new AddButtonDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
